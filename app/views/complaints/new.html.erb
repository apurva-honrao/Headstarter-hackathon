<!DOCTYPE html>
<html>
<head>
  <title>Complaint Form</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .form-container {
      background-color: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
      width: 400px;
      text-align: left;
      box-sizing: border-box;
    }

    h2 {
      text-align: center;
      margin-bottom: 25px;
      font-size: 26px;
      color: #333;
    }

    label {
      font-size: 16px;
      font-weight: bold;
      margin-top: 15px;
      display: block;
      color: #555;
    }

    input[type="text"], textarea {
      width: 100%;
      padding: 15px;
      margin: 10px 0 25px 0;
      border: 1px solid #ccc;
      border-radius: 8px;
      box-sizing: border-box;
    }

    textarea {
      resize: vertical;
      min-height: 150px;
      font-family: Arial, sans-serif;
      font-size: 16px;
      line-height: 1.5;
    }

    input[type="submit"] {
      width: 100%;
      padding: 15px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 18px;
    }

    input[type="submit"]:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h2>Submit Your Complaint</h2>
    <%= form_with model: @complaint, url: complaints_path, method: :post do |form| %>
      <label for="your_username">Your username</label>
      <%= form.text_field :username, placeholder: "Enter your username" %>

      <label for="your_complaint">Your Complaint</label>
      <%= form.text_area :complaint, placeholder: "Share your complaint here..." %>

      <%= form.submit "Submit Complaint" %>
    <% end %>
  </div>
</body>
</html>